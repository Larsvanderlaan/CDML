% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IC_DML.R
\name{estimate_IC_DML_bootstrap}
\alias{estimate_IC_DML_bootstrap}
\title{Bootstrap Calibrated Debiased Machine Learning Estimate (ICDML)}
\usage{
estimate_IC_DML_bootstrap(
  A,
  Y,
  mu1,
  mu0,
  pi1,
  pi0,
  nboot = 1000,
  folds = NULL,
  alpha = 0.05
)
}
\arguments{
\item{A}{A binary indicator variable (1 for treatment, 0 for control).}

\item{Y}{Observed outcomes.}

\item{mu1}{Predicted outcome for the treated group.}

\item{mu0}{Predicted outcome for the control group.}

\item{pi1}{Estimated propensity score for the treatment group (\code{A = 1}).}

\item{pi0}{Estimated propensity score for the control group (\code{A = 0}).}

\item{nboot}{Integer. Number of bootstrap samples (default is 1000).}

\item{folds}{List or \code{NULL}. A list of cross-validation folds, where each fold contains a validation set of indices. If \code{NULL}, a simple random sampling is used for each bootstrap iteration.}

\item{alpha}{Significance level for the confidence interval (default is 0.05).}
}
\value{
A list with the following elements:
\describe{
  \item{tau_n}{Estimated causal effect using ICDML.}
  \item{CI}{Bootstrap confidence interval for \code{tau_n}.}
}
}
\description{
Computes a bootstrap-assisted version of the ICDML estimator to obtain a confidence interval
for the causal effect, \eqn{\tau_n}, leveraging calibrated debiased machine learning.
}
\details{
This function estimates the causal effect using the ICDML estimator and computes a bootstrap confidence
interval by resampling within cross-validation folds. If \code{folds} is \code{NULL}, a simple random sample is drawn for each bootstrap iteration. Cross-fitting with bootstrapping allows for doubly robust inference, even when some nuisance function estimates are inconsistent or slow.

The confidence interval is constructed by calculating the empirical quantiles of the bootstrap distribution
of \code{tau_n}. The interval reflects the uncertainty of the ICDML estimate.
}
\examples{
\dontrun{
# Example usage
set.seed(123)
A <- rbinom(100, 1, 0.5)
Y <- rnorm(100, mean = A)
mu1 <- rep(1, 100)
mu0 <- rep(0, 100)
pi1 <- rep(0.5, 100)
pi0 <- rep(0.5, 100)
result <- estimate_IC_DML_bootstrap(A, Y, mu1, mu0, pi1, pi0, nboot = 500, alpha = 0.05)
print(result$tau_n)
print(result$CI)
}

}
\seealso{
\code{\link{estimate_IC_DML}}, \code{\link{calibrate_outcome_regression}}, \code{\link{calibrate_inverse_weights}}
}
